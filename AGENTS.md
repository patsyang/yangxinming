## 运行模式

- 用户消息以 `/` 开头时，视为项目命令调用，必须先读取 `commands/_dispatch.md` 查表定位目标，再按目标文件执行；不得猜测命令含义，不得跳过 dispatch 表。
- 非 `/` 开头的消息按普通讨论、设计、研究或代码任务处理；不得主动套用 skill 契约。
- 未指定输出位置时，所有临时文件、调试包、中间产物和一次性查询保存结果必须默认写入 `output/tmp/`；不得直接写入仓库根目录或 `output/` 根目录。已有 skill 明确规定的正式产物目录不受影响。

## 讨论姿态

本节约束判断方式，不覆盖 skill 契约、用户指定格式或必要的结构化输出。

用户的话是入口，不是结论。

先看它靠什么站住：哪一个前提、区分、对象关系或事实一旦不成立，后面的判断就会塌。没看见这个支点前，不急着给方案；看见之后，所有回答都从这个支点长出来。

讨论时少铺陈，少分类，少补全。不要为了显得完整而横向展开；只沿着最能承重的一刀往下切。能一句话说清，就不要列清单。能直接指出卡点，就不要包装成流程。

如果支点不明，就追问或说明卡在哪里。如果支点已明，就直接推进判断、取舍或方案。不要把思考过程写成固定模板。

## 输出前反证

话说出口前，先让它挨一刀。

找它最弱的前提、最危险的反例、最容易越界的地方。挨不住，就别原样输出：要么收窄，要么改写，要么承认不知道，要么继续追问。

不要把审查写成表演。真正的审查应体现在结果里：少一句虚话，多一分边界；少一个漂亮结论，多一个站得住的判断。

## 工作边界与内容边界

### 变更边界

- 测试脚本的防回归不能当作"事故纪念碑"。
- 拿着手动刀改代码，默认做最小必要改动：只修改完成当前请求所必需的源头文件、契约、生成器、测试或文档。
- 最小必要改动不等于只修当前报错点；涉及 skill 路由、schema、生成器、公共 helper、文档契约、knowledge 写入规则或多入口行为时，必须先定位规则源头，再修改源头或必要投影面。
- 改动前做有界一致性检查：搜索同名字段、同类 skill、同类生成路径、同类文档段落或同类测试；只扩大到同一规则实际影响的范围。
- 不得用特判当前输入、硬编码当前示例、跳过校验、降低质量门、删除失败测试或只改测试期望来掩盖系统性问题。
- 除非用户明确要求，不做顺手重构、顺手清理、顺手改名、目录迁移、抽象升级或格式化无关文件。
- 若全局正确修复超出当前请求范围，必须说明局部修复的覆盖范围、遗留风险和推荐源头修复点，不得把局部补丁描述成完整解决。

### 正式内容边界

- 正式 wiki、PRD、README、命令契约和对外文档只写应长期保留的事实、规则、结论和操作契约。
- 不得把推理过程、自我解释、调试记录、临时假设、AI 生成说明、占位内容或一次性示例值写入正式文档。
- 示例只能出现在明确需要示例的区域；示例必须是通用、合成、脱敏或 repo-relative 的值，不得使用用户本机路径、真实账号、真实环境名、真实密钥或当前调试值。
- 外部 knowledge 的 `state/archive/**` 只用于人工追溯，不属于正式知识；默认扫描、问题审计和查询结论应排除该目录，正式判断只以 `wiki/**`、`relations/**` 和当前有效 `state/**` 索引为准。

### 通用层中性化

- 本仓库的通用层不得写入项目个性化知识。
- `src/`、`tests/`、`contracts/`、`README.md`、`.trae/agents/` 中只能使用中性样例；不得出现来源业务系统名、模块代号、外部仓库名、真实业务功能名、真实业务枚举值、真实接口路径或用户本地工作区路径。
- 确需表达场景时，使用 `sample_knowledge`、`sample_code_knowledge`、`MODA/MODB/APPSET`、`示例能力` 等占位命名。
- 例外仅限本地运行配置中明确用于连接当前环境的 knowledge root，例如 `knowledge_kit.config.json`；这些值不得复制到测试、文档、模板、示例配置或生成产物。

## Knowledge 根目录

外部 knowledge 根目录在 `knowledge_kit.config.json` 中配置。

任何 knowledge 写入都必须遵守：

- 写入必须显式指定单个 knowledge 目标。
- 不得写入停用或只读 knowledge。
- 不得生成跨 knowledge 关系文件。
- 不得把正式知识正文迁移进本仓库。

## 验证口径

- 配置或 knowledge 注册关系变更后运行 `/k validate` 或 `python -m knowledge_kit validate`；它只校验配置和外部结构，不等同完整 LINT、verifier 或代码行为验证。
- knowledge 正式写入后运行对应 verifier；必要时运行 `/k lint -k <id>`。CLI `lint` 只是机械检查子集，不等同完整 Karpathy LINT。
- `/k update` 在正式 wiki 写入前必须先执行 preflight QUERY；preflight 通过不等于写入完成。
- `/k query`、`/k code`、`/k update` 遇到 `requires_*`、`slash_command_must_continue=true` 或 `final_answer_allowed=false` 时，不得把当前 artifact 当最终回答；必须按对应 contract 继续或报告阻断。
- 命令契约、生成器、schema、公共 helper 或代码行为变更后，运行受影响测试；不得用 `/k validate` 代替代码行为验证。
- 最终回答必须区分：已修改、已验证、未验证、阻断原因。

## Agent 协作规则

当需要执行 knowledge 维护或结论验证时，主 agent 必须读取对应的 agent prompt 并按照其中的指令执行：

- **Knowledge 维护**: 读取 `.trae/agents/knowledge-manager.md` 并按照其中的指令执行
- **结论验证**: 读取 `.trae/agents/conclusion-verifier.md` 并按照其中的指令执行
- **Knowledge 验证**: 读取 `.trae/agents/knowledge-verifier.md` 并按照其中的指令执行
- **KCode 分析**: 读取 `.trae/agents/kcode-analyzer.md` 并按照其中的指令执行
- **KCode 规划**: 读取 `.trae/agents/kcode-planner.md` 并按照其中的指令执行
- **KCode 验证**: 读取 `.trae/agents/kcode-verifier.md` 并按照其中的指令执行
